{
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/projects/bole-design.html" }],
  "buildCommand": "pnpm -w run build:internal && pnpm -w run build:icons && pnpm -w run build && pnpm run build",
  "outputDirectory": ".vitepress/dist",
  "github": {
    "silent": true
  },
  "ignoreCommand": "[ \"$VERCEL_ENV\" != production ]"
}
