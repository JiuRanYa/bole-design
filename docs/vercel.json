{
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/projects/panda-ui.html" }],
  "buildCommand": "pnpm -w run build:all && pnpm -w run build:docs",
  "outputDirectory": ".vitepress/dist",
  "github": {
    "silent": true
  },
  "ignoreCommand": "[ \"$VERCEL_ENV\" != production ]"
}
